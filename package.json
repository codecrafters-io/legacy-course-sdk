{
  "dependencies": {
    "ajv-cli": "^5.0.0",
    "prettier": "^3.0.3"
  },
  "scripts": {
    "lint:md": "prettier --prose-wrap=\"always\" --write \"courses/**/*.md\"",
    "lint:js": "prettier --write --ignore-path \"./.prettierignore\" \"courses/**/*.js\"",
    "lint": "npm run lint:md && npm run lint:js"
  }
}
